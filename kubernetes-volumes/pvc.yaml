apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc
  namespace: homework
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: my-storage
  resources:
    requests:
      storage: 1Gi